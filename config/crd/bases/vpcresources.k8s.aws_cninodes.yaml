
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.2
  creationTimestamp: null
  name: cninodes.vpcresources.k8s.aws
spec:
  group: vpcresources.k8s.aws
  names:
    kind: CNINode
    listKind: CNINodeList
    plural: cninodes
    shortNames:
    - cnd
    singular: cninode
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: The features delegated to VPC resource controller
      jsonPath: .spec.features
      name: Features
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: 'Important: Run "make" to regenerate code after modifying
              this file CNINodeSpec defines the desired state of CNINode'
            properties:
              features:
                items:
                  description: Feature is a type of feature being supported by VPC
                    resource controller and other AWS Services
                  properties:
                    name:
                      description: FeatureName is a type of feature name supported
                        by AWS VPC CNI. It can be Security Group for Pods, custom
                        networking, or others
                      type: string
                    value:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: CNINodeStatus defines the managed VPC resources.
            properties:
              branchenis:
                description: 'TODO: add VPC resources which will be managed by this
                  CRD and its finalizer'
                items:
                  properties:
                    id:
                      description: BranchENId is the network interface id of the branch
                        interface
                      type: string
                    ipv4Addr:
                      description: IPv4 and/or IPv6 address assigned to the branch
                        Network interface
                      type: string
                    ipv6Addr:
                      type: string
                    macAddr:
                      description: MacAdd is the MAC address of the network interface
                      type: string
                    subnetCIDR:
                      description: SubnetCIDR is the CIDR block of the subnet
                      type: string
                    subnetV6CIDR:
                      type: string
                    vlanId:
                      description: VlanId is the VlanId of the branch network interface
                      type: integer
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
